import 'package:flutter/material.dart';
import 'package:flutter_getx_wireframe/Config/themes/sizes.dart';

class AppSpaces {
  static const EdgeInsets globalPadding = EdgeInsets.all(AppSizes.size_16);
  static const EdgeInsets globalMargin = EdgeInsets.all(AppSizes.size_12);
  static const EdgeInsets smallPadding = EdgeInsets.all(AppSizes.size_08);
  static const EdgeInsets largePadding = EdgeInsets.all(AppSizes.size_24);
  static const EdgeInsets horizontalPadding = EdgeInsets.symmetric(horizontal: AppSizes.size_10);
  static const EdgeInsets verticalPadding = EdgeInsets.symmetric(vertical: AppSizes.size_10);
  static const EdgeInsets verticalAndHorizontalPadding = EdgeInsets.symmetric(vertical: AppSizes.size_10, horizontal: AppSizes.size_20);
}
